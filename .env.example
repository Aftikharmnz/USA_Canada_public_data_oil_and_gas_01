# Used only by server-side ingestion in GitHub Actions or a local refresh.
# Never prefix this variable with VITE_ and never expose it to browser code.
EIA_API_KEY=

# Set by the GitHub Pages build for project-repository hosting.
VITE_BASE_PATH=/

